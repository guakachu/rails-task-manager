<h2><%= "Name: #{@task.title}" %></h2>
<p><%= "Details : #{@task.details}" %></p>
<p><%= "Completed :#{@task.completed ? 'Yes' : 'No'}" %></p>

<%= link_to 'Edit this task', edit_path %><br>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%><br>
<%= link_to 'Back to list', root_path %>
